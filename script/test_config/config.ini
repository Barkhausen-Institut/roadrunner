[Python3:rrToolTest]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#pyRRToolTest --command bash\nsource $@

[Shell:rrToolTest]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#RRToolTest --command bash\nsource $@

[Shell:git]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#git --command bash\nsource $@

[Icarus]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#icarus --command bash\nsource $@

[Bender]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#bender --command bash\nsource $@

[Python3]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#python3 --command bash\nsource $@

[Git]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#git --command bash\nsource $@

[Slang]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#sv-lang --command bash\nsource $@

[Verilator]
exec = #!/usr/bin/env -S nix shell ${HOME}/.config/roadrunner#verilator --command bash\nsource $@
